export default class Todo {
  constructor(title, desc, date, priority, completed) {
    this.title = title;
    this.desc = desc;
    this.date = date;
    this.priority = priority;
    this.completed = completed;
  }
}
